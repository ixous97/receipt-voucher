# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# 위지엠(WJM) 지출결의서 증빙 자동화

영수증 사진을 넣으면 금액을 판독해 「지출결의서 증빙」 워드 문서를 만드는 **윈도우 프로그램**.
재정 담당자 **10~15명**이 각자 PC에서 exe로 쓴다. 코드·주석·식별자를 모두 한국어로 쓴다.

## 중요한 원칙

1. **AI(유료 API)를 쓰지 않는다.** Windows 내장 OCR로 실측 92%를 확인했고, 15명 배포에서
   결제·API 키 관리가 사라지는 이점이 크다. 붙이려면 비용 부담 주체와 키 배포 방식을 먼저 정해야 한다.
2. **검산 게이트를 우회하지 않는다.** 회계 문서다. `모델.검산()`을 통과하지 못하면 문서를 만들지 않는다.
   자동 판독값이 사람 확인 없이 결재 문서가 되는 일은 없어야 한다.
3. **원본 양식(`양식/원본/2026 지출결의 양식.docx`)은 읽기 전용이다.** 실제 업무 자료이자
   회귀 테스트의 정답지다. 수정하거나 옮기지 말 것.

## 개발 명령

```powershell
python -m pip install -r requirements.txt     # 준비
python src/main.py                            # 프로그램 실행
python src/main.py --자가진단 --표본 영수증    # 화면 없이 전 과정 점검
```

### 회귀 테스트 — 원본이 정답지다

원본 양식에 **완성된 결의서 3건과 그 영수증 12장**이 들어 있다. 이것이 회귀 테스트다.

```powershell
python 실측/재현검증.py    # 53,860 / 86,900 / 634,500 과 4매·2매·6매가 재현되어야 한다
python 실측/채점2.py       # OCR 정확도 11/12(92%) 유지 확인
```

실측 스크립트는 `src/core`의 판독 규칙을 그대로 불러 쓴다. 규칙을 고치면 채점 결과가 바로
따라오므로 **실측이 곧 회귀 테스트다.** 판독 규칙을 손봤다면 `실측/채점2.py`로 92%를 확인할 것.

문서 생성 코드를 고쳤다면 **페이지 수도 확인**한다(결의서 3건 = 3페이지).

```powershell
python -c "import sys; sys.path.insert(0,'src/core'); from pathlib import Path; import 미리보기, pypdfium2 as p; pdf=미리보기.pdf로(Path('산출물/재현검증.docx')); print(len(p.PdfDocument(str(pdf))))"
```

### exe 빌드

```powershell
python -m PyInstaller --noconfirm --onefile --windowed `
  --name "지출결의서 증빙 만들기" `
  --add-data "양식/결의서_템플릿.docx;양식" `
  --paths src/core --paths src/ui `
  --hidden-import winsdk.windows.media.ocr `
  --hidden-import winsdk.windows.globalization `
  --hidden-import winsdk.windows.graphics.imaging `
  --hidden-import winsdk.windows.storage `
  src/main.py
```

빌드 전에 실행 중인 exe를 반드시 끌 것 — 파일이 잠겨 있으면 `PermissionError`로 조용히 실패한다.
템플릿 경로는 `문서생성._자원()`이 `sys._MEIPASS`를 보고 알아서 찾는다.

## 아키텍처 — 데이터가 흐르는 길

```
영수증 폴더 (하위 폴더 = 한 쪽, 낱장들 = 한 쪽)
      │
      ▼  파이프라인.읽어오기()
   영수증판독.폴더읽기()  →  이미지정리.전처리()  →  영수증판독.판독()
                                                   금액·날짜·후보·확인필요
      ▼
   섹션 목록  ──────────  화면(메인창)에서 사람이 금액 확인·수정, 쪽 번호로 자리 이동
      │
      ▼  모델.검산()   ← 통과하지 못하면 여기서 멈춘다
      │
      ▼  파이프라인.문서만들기()
   모델.페이지나누기()  →  이미지정리.정규화()  →  문서생성.만들기()
   쪽 2개 = 문서 1장        삽입용 JPEG           템플릿 표를 복제해 채움
```

### 모듈 역할과 의존

| 모듈 | 역할 | 의존 |
|---|---|---|
| `core/모델.py` | 데이터 구조(`영수증`·`섹션`·`결의서`)와 `검산()`. **순수 파이썬, 외부 의존 없음** | — |
| `core/이미지정리.py` | HEIC/BMP→JPEG 정규화, OCR용 전처리 | Pillow |
| `core/영수증판독.py` | 폴더 스캔, Windows OCR 호출, 금액·날짜 추출 규칙 | 이미지정리, winsdk |
| `core/문서생성.py` | 템플릿 표 복제·확장, 이미지 삽입 | 모델, python-docx, Pillow |
| `core/파이프라인.py` | 위 넷을 이어 붙인 진입점. 화면은 이것만 부른다 | 위 전부 |
| `ui/메인창.py` | PySide6 화면. 판독은 `QThread`로 돌려 화면이 멈추지 않게 한다 | 파이프라인, 모델 |
| `core/미리보기.py` | 결과 검증용. Word COM으로 PDF 변환 후 이미지 렌더링 | pywin32, pypdfium2 |
| `core/템플릿추출.py` | 원본에서 빈 템플릿을 뽑는 1회성 도구 (44MB → 5.7KB) | python-docx |
| `core/설명서만들기.py` | 배포용 워드 사용설명서 생성 | python-docx |

`src/core`는 화면과 무관해 단독으로 테스트할 수 있다. 화면 없이 검증할 일이 대부분이므로
새 기능도 core에 먼저 넣고 화면을 나중에 붙이는 편이 빠르다.

### 폴더

```
양식/원본/               원본 양식 (읽기 전용, 44MB — 영수증 12장 포함)
양식/결의서_템플릿.docx   원본에서 이미지를 뺀 빈 템플릿 (5.7KB)
실측/                    OCR 정확도 측정 자료·보고서·회귀 테스트
배포/                    나눠 줄 꾸러미 (exe + 사용설명서)
산출물/ 영수증/          생성 결과 / 테스트 입력
```

---

## 삽질해서 알아낸 것들 — 고치기 전에 읽을 것

### 구조 — 쪽 번호 1~10, 두 쪽이 한 장

문서 한 장은 **왼쪽 쪽 / 오른쪽 쪽**으로 이루어지고 쪽 번호가 1부터 이어진다 —
1쪽=1장 왼쪽, 2쪽=1장 오른쪽, 3쪽=2장 왼쪽 … (`모델.쪽위치()`가 이 말을 만든다).
`모델.페이지나누기()`는 쪽을 두 개씩 묶기만 한다.

**영수증을 자동으로 옮기지 않는다.** 어디에 넣을지는 사람이 쪽 번호로 정하고,
한 쪽이 6장으로 차면 `섹션옮기기()`가 거절 사유를 문자열로 돌려준다(성공하면 빈 문자열).
예전에는 6장이 넘으면 자동으로 다음 장에 흘려보냈는데, 어디에 뭐가 들어갈지 예측할 수
없다는 지적을 받아 막는 방식으로 바꿨다.

제한값은 `모델.py` 맨 위에 모여 있다 — `최대섹션수=10`, `섹션최대장수=6`,
`섹션가로전환=4`, `쪽당섹션수=2`.

### 표는 늘 네 칸 — 좌우 폭이 정확히 반반

중첩표를 **항상 4칸**으로 만들고(`문서생성.기본칸수`), 왼쪽 두 칸 / 오른쪽 두 칸으로 나눈다.
그래야 한쪽이 한 줄만 써도 폭이 50:50으로 유지된다. 칸 수를 장수에 따라 바꾸면
왼쪽이 2/3을 차지하는 식으로 폭이 틀어진다.

- 한 쪽이 3장 이하면 그 쪽의 두 칸을 `_칸병합`으로 합쳐 한 칸처럼 쓴다
- 4장 이상이면 두 칸을 각각 쓴다 (`섹션.가로칸수()`)
- 왼쪽 쪽의 마지막 칸에 `_가운데선`으로 굵은 세로선을 그어 경계를 분명히 한다

**한 세로줄에는 한 쪽 것만 담는다.** 위쪽 설명과 아래 영수증이 세로로 맞아떨어져야
결재자가 어느 영수증이 어느 항목인지 알 수 있다. 한때 칸을 왼→오 순서로 채웠다가
헤더와 사진이 어긋난 적이 있으니 이 원칙을 깨지 말 것.

### 설명 문구는 자동으로 따라 바뀐다

`섹션.설명`에는 폴더명·파일명만 담고, 날짜와 건수는 `설명완성()`이 그때그때 붙인다.
그래서 영수증을 옮기면 문구가 자동으로 갱신된다. 사람이 화면에서 문구를 고치면
`직접입력=True`가 되어 그 뒤로는 자동 갱신하지 않는다.

### 워드 표

- **템플릿 헤더 행의 첫 칸은 `gridSpan=2`(2칸 병합)다.** 이걸 풀지 않으면 python-docx의
  `row.cells`가 같은 칸을 두 번 돌려줘 **왼쪽 설명이 오른쪽 것에 덮어써진다.**
  `_열맞추기()`가 병합을 푼 뒤, `_칸병합()`이 필요한 만큼 다시 합친다.
- 그림을 넣는 문단은 **여백(`space_before`/`space_after`)을 0으로** 둬야 한다.
  안 그러면 칸을 넘쳐 다음 페이지로 밀린다.
- **대지(R3) 높이는 첫 장과 이후 장이 달라야 한다.** 첫 장에는 "지출결의서 증빙" 제목이 얹혀
  세로 공간이 좁다. 첫 장 198mm / 이후 218mm. 원본값 219.8mm를 그대로 쓰면 첫 건이 2페이지로 밀린다.
- 중첩표 폭은 **중첩표 자신의 `tblW`(10896 dxa)** 를 써야 한다. 바깥 표 폭을 쓰면 살짝 넓어 넘친다.

### OCR / 금액 추출

- **OCR은 `15,000`을 `15, 000`으로 읽는다.** 콤마 뒤 공백을 허용해야 한다.
- **점(.)을 콤마로 보정하면 안 된다.** 날짜 `2026.08.27`이 금액으로 잡혀 정확도가 92%→83%로 떨어진다.
  (실제로 시도했다가 되돌린 이력)
- 날짜 구분자는 흘리거나(`2026.082718:32`) 엉뚱한 문자로 읽는다(`2026~08~27`). 구분자를 넓게 잡고
  월/일 범위로 거른다.
- 총액 고르기 우선순위: **키워드 → 검산(공급가+부가세=합계) → 최대 금액**. 마지막 폴백으로 고른 값은
  `확인필요=True`로 표시해 사람에게 넘긴다.
- 전처리(확대·회색조·대비강화·샤프닝)가 정확도를 67%→83%로 올린다. 반드시 거친다.

### 화면

- offscreen 렌더링에서는 **폰트 DB가 비어** 한글이 □로 보인다. 실제 실행과 무관하니 놀라지 말 것.
  화면을 캡처해 확인하려면 `Qt.WA_DontShowOnScreen` 속성으로 windows 플랫폼에서 렌더링한다.

---

## 실측 결과 (2026-09-06)

| 항목 | 결과 |
|---|---|
| 금액 판독 | 11/12 (92%) |
| 날짜 판독 | 11/12 (92%) |
| 후보 포함률 | 12/12 (100%) — 틀려도 클릭으로 해결 |
| 카드사 앱 캡처 / 감열지 사진 | 8/8 (100%) / 3/4 (75%) |
| 속도 · 비용 | 장당 1초 내외 · 0원 |

자세한 내용은 `실측/실측보고서.md`.

## exe 검증 방법

`--자가진단`을 붙여 실행하면 화면 없이 템플릿·OCR·검산·판독·문서생성을 점검하고 결과를
사용자 홈 폴더의 `지출결의서_자가진단.txt`에 남긴다. `--windowed` 빌드라 콘솔 출력이 없어
파일로 남기게 만든 것이다. 다른 PC에 배포한 뒤 이걸로 확인한다.

```
"지출결의서 증빙 만들기.exe" --자가진단 --표본 "영수증폴더경로"
```

이 PC에서는 전 항목 통과를 확인했다(2026-09-06). 템플릿도 번들 경로에서 정상적으로 찾았다.

## 아직 안 한 것

- **다른 PC에서** exe 실행 검증 (위 자가진단으로 확인할 수 있다)
- 지출인·날짜가 서로 다른 결의서를 한 문서에 담기
  (엔진은 지원한다 — `문서생성.만들기([결의서1, 결의서2, …])`. 화면이 한 건만 다룬다)

## 판독 규칙을 더 손대기 전에

표본이 **12장뿐**이다. 규칙을 더 얹으면 이 12장에는 맞고 실제 영수증에는 어긋나는 과최적화가 되기 쉽다.
실제로 점(.)을 콤마로 보정했다가 92%에서 83%로 떨어뜨린 이력이 있다. 실사용 영수증이 쌓인 뒤
그 데이터로 조정하는 편이 낫다.

남은 오답 1건(`표1_03` 이마트)은 영수증이 라벨/금액 두 덩어리로 분리 인식되는 경우다.
후보 목록에 정답이 들어 있어 사용자가 클릭으로 해결할 수 있으므로 급하지 않다.
