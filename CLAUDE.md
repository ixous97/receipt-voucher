# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# 위지엠(WJM) 지출결의서 증빙 자동화

영수증 사진을 넣으면 금액을 판독해 「지출결의서 증빙」 워드 문서를 만든다.
재정 담당자 **10~15명**이 쓴다. 코드·주석·식별자를 모두 한국어로 쓴다.

**같은 일을 하는 두 벌이 있다.**

| | 웹앱 (`web-app/`) | 윈도우판 (`src/`) |
|---|---|---|
| 판독 | Tesseract (WebAssembly) | Windows 내장 OCR |
| 금액 정확도 | 10/12 (83%) | 11/12 (92%) |
| 배포 | Vercel — 고치면 즉시 반영 | exe 를 15명에게 다시 배포 |
| 자료 | 브라우저 밖으로 안 나감 | PC 밖으로 안 나감 |
| 판독 시점 | 넣으면 곧바로 | [영수증 읽기] 를 눌러야 |
| HEIC(아이폰) | 못 읽는다 — 까닭을 알린다 | 읽는다 (pillow-heif) |

웹앱은 <https://receipt-voucher-khaki.vercel.app> 에 있다.
한때 GitHub Pages 로도 띄웠으나, Pages 는 뿌리나 `docs/` 만 게시할 수 있어
폴더 이름이 배포 수단에 묶이는 문제가 있었다. Vercel 은 Root Directory 를 지정할 수 있다.
그래서 예전 주소(`ixous97.github.io/receipt-voucher`)는 더 이상 살아 있지 않다.

## 중요한 원칙

0. **두 벌의 규칙을 함께 고친다.** `web-app/js/모델.js` 는 `src/core/모델.py` 를,
   `web-app/js/docx생성.js` 는 `src/core/문서생성.py` 를, `web-app/js/판독.js`·`전처리.js` 는
   `src/core/영수증판독.py`·`이미지정리.py` 를 옮긴 것이다. **한쪽만 고치면 같은 영수증으로
   서로 다른 문서가 나온다.** 고친 뒤에는 아래 '대조 시험'을 반드시 돌릴 것.
1. **AI(유료 API)를 쓰지 않는다.** Windows 내장 OCR로 실측 92%를 확인했고, 15명 배포에서
   결제·API 키 관리가 사라지는 이점이 크다. 붙이려면 비용 부담 주체와 키 배포 방식을 먼저 정해야 한다.
2. **검산 게이트를 우회하지 않는다.** 회계 문서다. `모델.검산()`을 통과하지 못하면 문서를 만들지 않는다.
   자동 판독값이 사람 확인 없이 결재 문서가 되는 일은 없어야 한다.
3. **원본 양식(`양식/원본/2026 지출결의 양식.docx`)은 읽기 전용이다.** 실제 업무 자료이자
   회귀 테스트의 정답지다. 수정하거나 옮기지 말 것.

## 개발 명령

```powershell
python -m pip install -r requirements.txt     # 준비
python src/main.py                            # 윈도우판 실행
python src/main.py --자가진단 --표본 영수증    # 화면 없이 전 과정 점검

python -m http.server 8765                     # 웹앱·검증 — 서버가 있어야 모듈이 뜬다
#   웹앱 → http://127.0.0.1:8765/web-app/
#   검증 → http://127.0.0.1:8765/검증/대조.html
```

**서버는 반드시 저장소 뿌리에서 띄운다.** 검증 페이지가 `../web-app/js/…` 로 웹앱을
가져다 쓰기 때문에, `--directory web-app` 으로 띄우면 검증 쪽이 아무것도 찾지 못한다.
웹앱을 `file://` 로 열면 브라우저가 모듈 로딩을 막는다.

### 대조 시험 — 두 벌이 같은 문서를 내는가

**문서 생성 규칙을 고쳤다면 반드시 돌린다.** 같은 입력으로 두 판을 돌려 결과를 견준다.

```powershell
python 검증/기준만들기.py     # 파이썬으로 기준 문서를 만든다
# 브라우저에서 http://127.0.0.1:8765/검증/대조.html 을 열고 대조실행() 실행
#   → 결과가 저장소 뿌리의 브라우저결과.b64 로 떨어진다
python 검증/대조하기.py       # 글자·표 격자·칸 병합·행 높이·그림 크기를 항목별로 견준다
```

마지막에 `두 문서의 구조가 완전히 같다` 가 나와야 한다. 인쇄 결과까지 픽셀 단위로
같음을 확인한 상태이므로, 어긋나면 그것은 이식 실수다.

판독 규칙을 고쳤다면 `검증/채점.html` 로 정확도를 다시 잰다. 정답표(`정답.js`)는
실제 지출 금액이라 저장소에 없다 — `정답.예시.js` 를 복사해 채워 쓴다.

### 회귀 테스트 — 원본이 정답지다

원본 양식에 **완성된 결의서 3건과 그 영수증 12장**이 들어 있다. 이것이 회귀 테스트다.

```powershell
python 실측/재현검증.py    # 53,860 / 86,900 / 634,500 과 4매·2매·6매가 재현되어야 한다
python 실측/채점2.py       # OCR 정확도 11/12(92%) 유지 확인
```

실측 스크립트는 `src/core`의 판독 규칙을 그대로 불러 쓴다. 규칙을 고치면 채점 결과가 바로
따라오므로 **실측이 곧 회귀 테스트다.** 판독 규칙을 손봤다면 `실측/채점2.py`로 92%를 확인할 것.

문서 생성 코드를 고쳤다면 **페이지 수도 확인**한다(결의서 3건 = 3페이지).

```powershell
python -c "import sys; sys.path.insert(0,'src/core'); from pathlib import Path; import 미리보기, pypdfium2 as p; pdf=미리보기.pdf로(Path('산출물/재현검증.docx')); print(len(p.PdfDocument(str(pdf))))"
```

### exe 빌드

```powershell
python -m PyInstaller --noconfirm --onefile --windowed `
  --name "지출결의서 증빙 만들기" `
  --add-data "양식/결의서_템플릿.docx;양식" `
  --paths src/core --paths src/ui `
  --hidden-import winsdk.windows.media.ocr `
  --hidden-import winsdk.windows.globalization `
  --hidden-import winsdk.windows.graphics.imaging `
  --hidden-import winsdk.windows.storage `
  src/main.py
```

빌드 전에 실행 중인 exe를 반드시 끌 것 — 파일이 잠겨 있으면 `PermissionError`로 조용히 실패한다.
템플릿 경로는 `문서생성._자원()`이 `sys._MEIPASS`를 보고 알아서 찾는다.

## 아키텍처 — 데이터가 흐르는 길

```
영수증 폴더 (하위 폴더 = 한 쪽, 낱장들 = 한 쪽)
      │
      ▼  파이프라인.읽어오기()
   영수증판독.폴더읽기()  →  이미지정리.전처리()  →  영수증판독.판독()
                                                   금액·날짜·후보·확인필요
      ▼
   섹션 목록  ──────────  화면(메인창)에서 사람이 금액 확인·수정, 쪽 번호로 자리 이동
      │
      ▼  모델.검산()   ← 통과하지 못하면 여기서 멈춘다
      │
      ▼  파이프라인.문서만들기()
   모델.페이지나누기()  →  이미지정리.정규화()  →  문서생성.만들기()
   쪽 2개 = 문서 1장        삽입용 JPEG           템플릿 표를 복제해 채움
```

### 모듈 역할과 의존

| 모듈 | 역할 | 의존 |
|---|---|---|
| `core/모델.py` | 데이터 구조(`영수증`·`섹션`·`결의서`)와 `검산()`. **순수 파이썬, 외부 의존 없음** | — |
| `core/이미지정리.py` | HEIC/BMP→JPEG 정규화, OCR용 전처리 | Pillow |
| `core/영수증판독.py` | 폴더 스캔, Windows OCR 호출, 금액·날짜 추출 규칙 | 이미지정리, winsdk |
| `core/문서생성.py` | 템플릿 표 복제·확장, 이미지 삽입 | 모델, python-docx, Pillow |
| `core/파이프라인.py` | 위 넷을 이어 붙인 진입점. 화면은 이것만 부른다 | 위 전부 |
| `ui/메인창.py` | PySide6 화면. 판독은 `QThread`로 돌려 화면이 멈추지 않게 한다 | 파이프라인, 모델 |
| `core/미리보기.py` | 결과 검증용. Word COM으로 PDF 변환 후 이미지 렌더링 | pywin32, pypdfium2 |
| `core/템플릿추출.py` | 원본에서 빈 템플릿을 뽑는 1회성 도구 (44MB → 5.7KB) | python-docx |
| `core/설명서만들기.py` | 배포용 워드 사용설명서 생성 | python-docx |

`src/core`는 화면과 무관해 단독으로 테스트할 수 있다. 화면 없이 검증할 일이 대부분이므로
새 기능도 core에 먼저 넣고 화면을 나중에 붙이는 편이 빠르다.

### 웹앱 모듈 — 파이썬과 짝이 맞는다

| 웹앱 | 짝이 되는 파이썬 | 다른 점 |
|---|---|---|
| `web-app/js/모델.js` | `core/모델.py` | 없음. 값과 규칙이 1:1 |
| `web-app/js/docx생성.js` | `core/문서생성.py` | python-docx 가 없어 zip·관계파일·그림 XML 을 직접 조립 |
| `web-app/js/전처리.js` | `core/이미지정리.py` | PIL 대신 캔버스. 흐림은 `ctx.filter=blur()` 를 빌려 쓴다 |
| `web-app/js/판독.js` | `core/영수증판독.py` | 엔진만 Tesseract(WASM). 금액·날짜 규칙은 같다 |
| `web-app/js/앱.js` | `ui/메인창.py` | 화면 조작 방식만 다르다 |

`web-app/vendor/` 에는 JSZip, Tesseract.js, 한국어 학습 데이터(12MB)가 들어 있다.
**학습 데이터가 빠지면 판독이 통째로 죽는다.** CDN 을 쓰지 않고 저장소에 둔 것은
바깥 서비스가 멈춰도 프로그램이 계속 돌게 하기 위해서다.

### 폴더

```
src/                     윈도우판 (core = 규칙, ui = 화면)
web-app/                 웹앱. 이 폴더만 Vercel 에 배포된다
  index.html js/ vendor/ 양식/   화면 / 코드 / 라이브러리·학습데이터 / 템플릿
검증/                    대조 시험·채점 도구. **배포되지 않는다**
양식/원본/               원본 양식 (읽기 전용, 44MB — 영수증 12장 포함)
양식/결의서_템플릿.docx   원본에서 이미지를 뺀 빈 템플릿 (5.7KB)
실측/                    OCR 정확도 측정 자료·보고서·회귀 테스트
배포/                    나눠 줄 꾸러미 (exe + 사용설명서)
산출물/ 영수증/ 테스트/   생성 결과 / 테스트 입력 / 손으로 굴려 본 자료
```

**`web-app/` 에는 배포될 것만 둔다.** 검증 도구를 그 안에 두면 실제 영수증과 지출 금액
정답표가 그대로 배포된다. `.vercelignore` 는 지금 CLI(59.11.7)에서는 `vercel build` 에도
적용되지만(2026-09-07 실측), 패턴 하나 잘못 쓰면 필요한 파일까지 지운다 —
**설정 파일을 믿지 말고 폴더를 나눌 것.** 그물은 그물일 뿐이다.

**저장소는 공개다** — <https://github.com/ixous97/receipt-voucher>.
실제 회계 자료(영수증·산출물·원본 양식·배포 꾸러미)와 채점 정답표는 `.gitignore` 로 막혀 있다.
새 파일을 둘 때 그것이 실제 지출 자료라면 반드시 무시 목록에 먼저 넣을 것.

### 배포 — Vercel

Vercel 프로젝트 설정에서 **Root Directory 를 `web-app` 으로** 두어야 한다. 그러지 않으면
저장소 뿌리의 `src/main.py`(PySide6 진입점)와 `requirements.txt` 를 보고 Python 프로젝트로
감지해 `app` 변수를 찾다가 실패한다 — 실제로 그 오류를 만났다.

```
Framework Preset   Other        Build Command     (비움)
Root Directory     web-app      Output Directory  (비움)
```

서버가 필요 없는 정적 사이트다. 백엔드도 빌드 과정도 없다.

`vercel.json` 이 **보안 헤더**를 모든 경로에 붙인다 — CSP·`X-Content-Type-Options`·
`Referrer-Policy`·`Permissions-Policy`·`COOP`. CSP 의 `connect-src 'self'` 가
"영수증이 브라우저를 벗어나지 않는다"를 문서가 아니라 **브라우저가 강제하게** 만든다.
헤더를 고쳤다면 배포 전에 반드시 브라우저로 전 과정을 굴려 볼 것(아래).
배포될 파일은 열세 개뿐이며, 로컬에서 그대로 확인할 수 있다.

```powershell
cd web-app; npx vercel build          # .vercel/output/static 에 배포될 것만 모인다
python -m http.server 8767 --directory web-app/.vercel/output/static
```

빌드 산출물에 `*.py` 나 영수증이 섞였다면 **배포하지 말 것.** 그건 폴더 분리가 무너졌다는 뜻이다.

윈도우판은 여전히 exe 를 다시 만들어 나눠 줘야 한다. 그래서 급한 수정은 웹앱에 먼저 넣고,
윈도우판은 모아서 내보내는 편이 낫다 — 단, **규칙을 고쳤다면 두 벌을 함께 고쳐야 한다**(원칙 0).

---

## 삽질해서 알아낸 것들 — 고치기 전에 읽을 것

### 구조 — 쪽 번호 1~10, 두 쪽이 한 장

문서 한 장은 **왼쪽 쪽 / 오른쪽 쪽**으로 이루어지고 쪽 번호가 1부터 이어진다 —
1쪽=1장 왼쪽, 2쪽=1장 오른쪽, 3쪽=2장 왼쪽 … (`모델.쪽위치()`가 이 말을 만든다).
`모델.페이지나누기()`는 쪽을 두 개씩 묶기만 한다.

**영수증을 자동으로 옮기지 않는다.** 어디에 넣을지는 사람이 쪽 번호로 정하고,
한 쪽이 6장으로 차면 `섹션옮기기()`가 거절 사유를 문자열로 돌려준다(성공하면 빈 문자열).
예전에는 6장이 넘으면 자동으로 다음 장에 흘려보냈는데, 어디에 뭐가 들어갈지 예측할 수
없다는 지적을 받아 막는 방식으로 바꿨다.

제한값은 `모델.py` 맨 위에 모여 있다 — `최대섹션수=10`, `섹션최대장수=6`,
`섹션가로전환=4`, `쪽당섹션수=2`.

### 표는 늘 네 칸 — 좌우 폭이 정확히 반반

중첩표를 **항상 4칸**으로 만들고(`문서생성.기본칸수`), 왼쪽 두 칸 / 오른쪽 두 칸으로 나눈다.
그래야 한쪽이 한 줄만 써도 폭이 50:50으로 유지된다. 칸 수를 장수에 따라 바꾸면
왼쪽이 2/3을 차지하는 식으로 폭이 틀어진다.

- 한 쪽이 3장 이하면 그 쪽의 두 칸을 `_칸병합`으로 합쳐 한 칸처럼 쓴다
- 4장 이상이면 두 칸을 각각 쓴다 (`섹션.가로칸수()`)
- 왼쪽 쪽의 마지막 칸에 `_가운데선`으로 굵은 세로선을 그어 경계를 분명히 한다

**한 세로줄에는 한 쪽 것만 담는다.** 위쪽 설명과 아래 영수증이 세로로 맞아떨어져야
결재자가 어느 영수증이 어느 항목인지 알 수 있다. 한때 칸을 왼→오 순서로 채웠다가
헤더와 사진이 어긋난 적이 있으니 이 원칙을 깨지 말 것.

### 설명 문구는 자동으로 따라 바뀐다

`섹션.설명`에는 폴더명·파일명만 담고, 날짜와 건수는 `설명완성()`이 그때그때 붙인다.
그래서 영수증을 옮기면 문구가 자동으로 갱신된다. 사람이 화면에서 문구를 고치면
`직접입력=True`가 되어 그 뒤로는 자동 갱신하지 않는다.

### 워드 표

- **템플릿 헤더 행의 첫 칸은 `gridSpan=2`(2칸 병합)다.** 이걸 풀지 않으면 python-docx의
  `row.cells`가 같은 칸을 두 번 돌려줘 **왼쪽 설명이 오른쪽 것에 덮어써진다.**
  `_열맞추기()`가 병합을 푼 뒤, `_칸병합()`이 필요한 만큼 다시 합친다.
- 그림을 넣는 문단은 **여백(`space_before`/`space_after`)을 0으로** 둬야 한다.
  안 그러면 칸을 넘쳐 다음 페이지로 밀린다.
- **대지(R3) 높이는 첫 장과 이후 장이 달라야 한다.** 첫 장에는 "지출결의서 증빙" 제목이 얹혀
  세로 공간이 좁다. 첫 장 198mm / 이후 218mm. 원본값 219.8mm를 그대로 쓰면 첫 건이 2페이지로 밀린다.
- 중첩표 폭은 **중첩표 자신의 `tblW`(10896 dxa)** 를 써야 한다. 바깥 표 폭을 쓰면 살짝 넓어 넘친다.

### OCR / 금액 추출

- **OCR은 `15,000`을 `15, 000`으로 읽는다.** 콤마 뒤 공백을 허용해야 한다.
- **점(.)을 콤마로 보정하면 안 된다.** 날짜 `2026.08.27`이 금액으로 잡혀 정확도가 92%→83%로 떨어진다.
  (실제로 시도했다가 되돌린 이력)
- 날짜 구분자는 흘리거나(`2026.082718:32`) 엉뚱한 문자로 읽는다(`2026~08~27`). 구분자를 넓게 잡고
  월/일 범위로 거른다.
- 총액 고르기 우선순위: **키워드 → 검산(공급가+부가세=합계) → 최대 금액**. 마지막 폴백으로 고른 값은
  `확인필요=True`로 표시해 사람에게 넘긴다.
- 전처리(확대·회색조·대비강화·샤프닝)가 정확도를 67%→83%로 올린다. 반드시 거친다.

### 화면

- offscreen 렌더링에서는 **폰트 DB가 비어** 한글이 □로 보인다. 실제 실행과 무관하니 놀라지 말 것.
  화면을 캡처해 확인하려면 `Qt.WA_DontShowOnScreen` 속성으로 windows 플랫폼에서 렌더링한다.

### 웹앱

- **lxml 의 `find`/`findall`(직계 자식)과 `iter`(후손 전부) 구분을 DOM 에서도 지켜야 한다.**
  `web-app/js/docx생성.js` 의 `자식들()`·`후손들()` 이 그 구분이다. 뭉뚱그려 후손 검색을 쓰면
  중첩표의 행까지 딸려 와 표가 망가진다.
- **zip 안 파트 이름은 ASCII 로 둔다**(`image1.jpg`). 워드 규격이 권하는 바다.
- 그림을 넣으려면 세 가지를 모두 해야 한다 — `word/media` 에 바이트 넣기, `document.xml.rels`
  에 관계 등록, 그 rId 를 가리키는 drawing XML 조립. 하나라도 빠지면 워드가 파일을 거부한다.
- 글자를 쓸 때 `xml:space="preserve"` 를 반드시 붙인다. `( 지출인 :  )` 같은 앞뒤 공백이 사라진다.
- **배포 폴더에는 배포될 것만 둔다.** 검증 도구를 `web-app/` 안에 두었더니 실제 영수증과
  지출 금액 정답표가 빌드 산출물에 그대로 딸려 들어갔다. 설정 파일이 아니라 폴더 구조로 막을 것.
- **`.vercelignore` 에서 `.docx` 를 확장자로 막지 말 것.** `*.docx` 도, 뿌리 고정을 노린
  `/*.docx` 도 `양식/결의서_템플릿.docx` 까지 함께 지워 문서 생성이 죽는다 — **둘 다 겪었다.**
  `.gitignore` 와 달리 앞의 `/` 를 뿌리 고정으로 보지 않고, 부정 패턴(`!`)으로 되살리는 것도
  먹지 않는다. 그래서 앱이 내려주는 이름(`*지출결의서증빙.docx`, `앱.js:337`)만 콕 집어 막는다.
- **`.vercelignore` 는 `vercel build` 에도 적용된다**(CLI 59.11.7 실측). 예전에 적어 둔
  "빌드 단계에서는 안 먹는다"는 옛 CLI 이야기다. 고칠 때마다 `npx vercel build` 로
  **산출물 13개**(index.html·js 5·vendor 6·양식 1)를 눈으로 확인할 것.
- **`vercel.json` 은 주석 키(`"//"`)를 거부한다.** `headers[0]` 에 넣었더니
  `Invalid vercel.json - should NOT have additional property` 로 빌드가 실패했다. 설명은 여기 적을 것.
- **CSP 를 조일 때 판독이 조용히 죽는다.** `script-src` 의 `'wasm-unsafe-eval'`(Tesseract WASM),
  `blob:`(워커), `worker-src blob:` 중 하나만 빠져도 OCR 이 통째로 멈춘다. `COEP` 는 넣지 않는다.
  `connect-src 'self'` 가 이 앱의 핵심이다 — 영수증을 밖으로 보낼 통로를 브라우저가 막는다.
- Vercel 은 저장소 뿌리에 `requirements.txt` 와 `src/main.py` 가 있으면 Python 프로젝트로
  보고 `app` 변수를 찾는다. **Root Directory 를 `web-app` 으로 지정**하면 그 폴더만 본다.
- 학습 데이터는 IndexedDB(`keyval-store`)에 캐시된다. 데이터를 바꿔 시험할 때는 지워야 새로 받는다.

---

## 실측 결과 — 같은 영수증 12장으로 잰 것

| 항목 | 윈도우 내장 OCR | Tesseract (파이썬) | Tesseract (브라우저) |
|---|---|---|---|
| 금액 | **11/12 (92%)** | 11/12 (92%) | 10/12 (83%) |
| 날짜 | 11/12 (92%) | 11/12 (92%) | 11/12 (92%) |
| 후보 포함률 | 12/12 (100%) | 11/12 (92%) | 11/12 (92%) |
| 속도 | 장당 1초 | — | 장당 4초 |

셋 다 비용 0원이다. 자세한 내용은 `실측/실측보고서.md`.

**두 엔진이 서로 다른 것을 틀린다.** 내장 OCR 은 `표1_03`(이마트)을 틀리고 `표1_02`를 맞히며,
Tesseract 는 그 반대다. **둘을 함께 쓰면 12/12 가 된다** — 윈도우판에 나중에 얹어 볼 만하다.

브라우저가 파이썬보다 금액 한 장이 낮은 것은 학습 데이터 탓이 아니다(같은 파일로 바꿔
다시 재도 결과가 같았다). 캔버스와 PIL 의 이미지 처리 미세 차이로 OCR 원문이 조금 달라지는
것으로 보인다. 틀린 장도 정답이 후보에 들어 있어 금액 칸 목록에서 고르면 해결된다.

## exe 검증 방법

`--자가진단`을 붙여 실행하면 화면 없이 템플릿·OCR·검산·판독·문서생성을 점검하고 결과를
사용자 홈 폴더의 `지출결의서_자가진단.txt`에 남긴다. `--windowed` 빌드라 콘솔 출력이 없어
파일로 남기게 만든 것이다. 다른 PC에 배포한 뒤 이걸로 확인한다.

```
"지출결의서 증빙 만들기.exe" --자가진단 --표본 "영수증폴더경로"
```

이 PC에서는 전 항목 통과를 확인했다(2026-09-06). 템플릿도 번들 경로에서 정상적으로 찾았다.

## 아직 안 한 것

양쪽 모두
- 지출인·날짜가 서로 다른 결의서를 한 문서에 담기
  (엔진은 두 판 모두 지원한다 — `만들기([결의서1, 결의서2, …])`. 화면이 한 건만 다룬다)
- **두 판독 엔진을 함께 쓰기.** 서로 다른 것을 틀리므로 합치면 12/12 가 된다(아래 실측 참고)

웹앱
- **폴더 단위로 넣기.** 윈도우판은 하위 폴더 하나가 한 쪽이 되지만, 웹앱은 파일만 받아
  넣은 순서대로 6장씩 채운다. `webkitdirectory` 로 폴더를 받을 수 있지만 아직 안 붙였다
- **HEIC 읽기.** 브라우저가 아이폰 기본 형식을 열지 못한다. 지금은 까닭을 알려 주기만 한다.
  heic 디코더(WASM)를 얹으면 되지만 용량이 는다 — 학습 데이터 12MB 에 더 얹을 값어치인지 따져 볼 것

윈도우판
- **다른 PC에서** exe 실행 검증 (위 자가진단으로 확인할 수 있다)
- 웹앱에 넣은 개선(넣으면 곧바로 판독)을 윈도우판에도 옮기기

## 판독 규칙을 더 손대기 전에

표본이 **12장뿐**이다. 규칙을 더 얹으면 이 12장에는 맞고 실제 영수증에는 어긋나는 과최적화가 되기 쉽다.
실제로 점(.)을 콤마로 보정했다가 92%에서 83%로 떨어뜨린 이력이 있다. 실사용 영수증이 쌓인 뒤
그 데이터로 조정하는 편이 낫다.

남은 오답은 판이 서로 다르다. 윈도우판은 `표1_03`(이마트) 하나, 웹앱은 `표1_02`·`표1_03`
둘이다. 어느 쪽이든 **정답이 후보 목록에 들어 있어** 금액 칸에서 골라 해결할 수 있으므로
급하지 않다. 규칙을 손대는 것보다 두 엔진을 합치는 편이 이득이 크다.
