# ── 실제 회계 자료. 절대 올리지 않는다 ───────────────────────
# 실명·지출 내역·영수증 사진이 들어 있어 공개 저장소에 올라가면 안 된다.
산출물/
영수증/
테스트/
실측/
양식/원본/
배포/
붙여넣은영수증/

# 작업 중 만들어지는 사본
**/_작업/
**/_전처리/
**/_테서랙트전처리/
**/처리완료/

# 빌드 결과물
build/
dist/
*.spec
__pycache__/
*.pyc
.venv/
venv/

# 웹앱이 내려받아 쓰는 학습 데이터 (12MB, 첫 실행 때 받는다)
docs/**/*.traineddata

# 대조 시험이 만들어 내는 것들 (기준만들기.py 를 돌리면 다시 생긴다)
docs/검증/그림/
docs/검증/_*/
docs/검증/*.docx
docs/검증/*.pdf
docs/검증/차이.png
브라우저결과.b64
.playwright-mcp/
docs/검증/영수증/

# 실제 지출 금액이 담긴 정답표 (정답.예시.js 를 복사해 만든다)
docs/검증/정답.js
