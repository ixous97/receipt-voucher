# ── 실제 회계 자료. 절대 올리지 않는다 ───────────────────────
# 실명·지출 내역·영수증 사진이 들어 있어 공개 저장소에 올라가면 안 된다.
산출물/
영수증/
테스트/
실측/
양식/원본/
배포/
붙여넣은영수증/

# 작업 중 만들어지는 사본
**/_작업/
**/_전처리/
**/_테서랙트전처리/
**/처리완료/

# 빌드 결과물
build/
dist/
*.spec
__pycache__/
*.pyc
.venv/
venv/

# 웹앱이 내려받아 쓰는 학습 데이터 (12MB, 첫 실행 때 받는다)
web/**/*.traineddata

# 대조 시험이 만들어 내는 것들 (기준만들기.py 를 돌리면 다시 생긴다)
web/검증/그림/
web/검증/_*/
web/검증/*.docx
web/검증/*.pdf
web/검증/차이.png
브라우저결과.b64
.playwright-mcp/
web/검증/영수증/
